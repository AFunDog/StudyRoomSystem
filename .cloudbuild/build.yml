# This YAML is the default template and can be modified based on this 
---
version: 2.0
steps:
  PRE_BUILD:
  - checkout:
      name: "checkout"
      inputs:
        scm: "codehub"
        url: "git@codehub.devcloud.cn-north-4.huaweicloud.com:8552fa04ec35429c9c4a5a35abb5f364/StudyRoomSystem.git"
        branch: "main"
        lfs: false
        submodule: false
  BUILD:
  - sh:
      name: "sh"
      inputs:
        command: "docker build -f .\scripts\server.Dockerfile -t study_room_system:latest ."
